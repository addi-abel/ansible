---

ansible_become:        'yes'
ansible_become_method: 'sudo'
ansible_ssh_user:      'addi'

domain_realm:          'abel.de.com'
dns_domain:            "{{ domain_realm }}"
dns_search_domains:    "{{ dns_domain }}"
